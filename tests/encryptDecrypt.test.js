const chai = require("chai");
const expect = chai.expect;

const passPhrase = "i like CryptoJs";

describe("crypto", () => {
    it("should encrypt test", () => {
        let actual = true;

        expect(actual).to.equal(true);
    });

    it("should decrypt test", () => {
        let actual = true;

        expect(actual).to.equal(true);
    });
});